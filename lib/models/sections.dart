import 'package:flutter/material.dart';

class Sections {
  String title;
  double value;
  int index;
  Color color;
  Sections({
    required this.title,
    required this.value,
    required this.index,
    required this.color,
  });
}
